// WAP to check value is divisible by 5
#include<stdio.h>
int main()
{
    int no;
    printf("Enter the value of no:");
    scanf("%d", &no);

    if (no%5 == 0)
    {
        printf("\nNo is divisible by 5.");
    }
    
}