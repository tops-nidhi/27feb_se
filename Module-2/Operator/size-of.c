#include<stdio.h>
int main()
{
    int a = 1;
    int size;
    //Size is variable
    // sizeof is operator 
    size = sizeof(char);
    printf("\nSize of the a is:%d", size);
    return 0;
}