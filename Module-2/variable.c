#include<stdio.h>
void main()
{
   /*
        int no = 50;
        printf("\nValue of no is:%d", no);
   */
    char no;
    printf("Enter the value of no:");
    scanf("%c", &no);
    printf("\nValue of no is:%c", no);
}